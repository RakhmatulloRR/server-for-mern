const home = require('./home');
const books = require('./books');
const users = require('./users');
const auth = require('./auth');
const customers = require('./customers');

module.exports = {home, books, users, auth, customers};